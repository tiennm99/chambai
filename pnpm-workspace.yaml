allowBuilds:
  sharp: true
  unrs-resolver: true

overrides:
  js-yaml: '>=5.2.2'
  postcss: '>=8.5.18'
  '@babel/core': '>=7.29.6 <8'
  # next declares sharp ^0.34.5, which excludes the patched 0.35.0, so no
  # upstream bump can reach the fix. Bounded below 1 because 0.x majors break.
  sharp: '>=0.35.0 <1'
